import { MetaSettingsClient } from "@/components/meta-settings-client";

export const metadata = {
  title: "Paramètres Facebook - Simon Morin - Agent de location",
};

export default function MetaSettingsPage() {
  return <MetaSettingsClient />;
}
